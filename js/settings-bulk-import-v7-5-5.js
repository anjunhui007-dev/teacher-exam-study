(() => {
'use strict';
const DATA='tes_curriculum_v5',SETTINGS='tes_settings_v5';
const $=s=>document.querySelector(s),read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k))??JSON.parse(JSON.stringify(f))}catch{return JSON.parse(JSON.stringify(f))}},write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));
let chosen=[];
function css(){if($('#v755bulkstyle'))return;const s=document.createElement('style');s.id='v755bulkstyle';s.textContent='.v755bulkbox{border:1px dashed var(--line);border-radius:12px;padding:14px}.v755bulkrow{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.v755bulkfiles{font-size:12px;color:var(--muted);margin-top:9px;line-height:1.65}.v755bulkstatus{margin-top:10px;padding:10px 12px;border-radius:10px;background:#f7f8fa;font-size:12px;line-height:1.6;white-space:pre-wrap}.v755bulkstatus.good{background:#f0fdf4;color:#166534}.v755bulkstatus.bad{background:#fff1f2;color:#9f1239}.v755bulkbox input{display:none}';document.head.append(s)}
function inject(){const form=$('#settingsForm');if(!form)return;$('#v754BulkSection')?.remove();$('#v75BulkSection')?.remove();if($('#v755BulkSection'))return;const anchor=[...form.querySelectorAll('.settings-section')].find(x=>x.querySelector('h3')?.textContent.includes('AI API'))||form.querySelector('.dialog-actions');const sec=document.createElement('section');sec.className='settings-section';sec.id='v755BulkSection';sec.innerHTML='<h3>교육과정 본문 JSON 일괄 업로드</h3><p class="muted">큰 본문 데이터는 IndexedDB에 저장되어 모바일에서도 용량 제한을 피합니다. 표 JSON은 표 모음집에서 업로드합니다.</p><div class="v755bulkbox"><div class="v755bulkrow"><input id="v755BulkFiles" type="file" multiple accept="application/json,.json"><button type="button" id="v755PickBulk" class="ghost-btn">본문 JSON 선택</button><button type="button" id="v755InspectBulk" class="ghost-btn" disabled>내용 확인</button><button type="button" id="v755ApplyBulk" class="primary-btn" disabled>일괄 적용</button></div><div id="v755BulkList" class="v755bulkfiles">선택된 파일 없음</div><div id="v755BulkStatus" class="v755bulkstatus" hidden></div></div>';form.insertBefore(sec,anchor);$('#v755PickBulk').onclick=()=>$('#v755BulkFiles').click();$('#v755BulkFiles').onchange=e=>{chosen=[...e.target.files];$('#v755BulkList').innerHTML=chosen.length?chosen.map(f=>'• '+f.name).join('<br>'):'선택된 파일 없음';$('#v755InspectBulk').disabled=!chosen.length;$('#v755ApplyBulk').disabled=true;$('#v755BulkStatus').hidden=true};$('#v755InspectBulk').onclick=inspect;$('#v755ApplyBulk').onclick=apply}
function extractSubjects(p){if(p?.contentType==='curriculum-tables')throw new Error('표 JSON입니다. 표 모음집에서 업로드해 주세요.');if(p?.subject&&typeof p.subject==='object')return[p.subject];if(Array.isArray(p?.subjects)&&p.subjects.length)return p.subjects;if(p?.id&&Array.isArray(p?.sections))return[p];throw new Error('과목 데이터(subject/subjects)를 찾을 수 없습니다.')}
async function parseChosen(){const subjects=[],errors=[];for(const f of chosen){try{const p=JSON.parse(await f.text()),arr=extractSubjects(p);for(const s of arr){if(!s?.id||!Array.isArray(s.sections))throw new Error('subject.id 또는 sections가 없습니다.');subjects.push({subject:s,file:f.name})}}catch(e){errors.push(`${f.name}: ${e.message||e}`)}}const dedup=new Map();subjects.forEach(x=>dedup.set(x.subject.id,x));return{subjects:[...dedup.values()],errors}}
async function inspect(){const r=await parseChosen(),box=$('#v755BulkStatus');box.hidden=false;const lines=[`인식된 과목: ${r.subjects.length}개`,...r.subjects.map(x=>`✓ ${x.subject.name||x.subject.id} (${x.subject.id}) ← ${x.file}`)];if(r.errors.length)lines.push('',`오류: ${r.errors.length}개`,...r.errors.map(x=>'✕ '+x));box.textContent=lines.join('\n');box.className='v755bulkstatus '+(r.subjects.length?'good':'bad');$('#v755ApplyBulk').disabled=!r.subjects.length}
async function apply(){const btn=$('#v755ApplyBulk');btn.disabled=true;btn.textContent='저장 중…';const r=await parseChosen();if(!r.subjects.length){btn.textContent='일괄 적용';await inspect();return}const d=read(DATA,{schemaVersion:5,subjects:[]});d.subjects??=[];for(const{subject:s}of r.subjects){const i=d.subjects.findIndex(x=>x.id===s.id),clean={...s,reviewItems:s.reviewItems||[]};if(i>=0)d.subjects[i]=clean;else d.subjects.push(clean)}d.schemaVersion=Math.max(Number(d.schemaVersion)||5,8);try{write(DATA,d);await window.TESLargeStore.flush();const verify=read(DATA,{subjects:[]});const missing=r.subjects.filter(({subject:s})=>!verify.subjects?.some(x=>x.id===s.id&&Array.isArray(x.sections)));if(missing.length)throw new Error('저장 검증 실패: '+missing.map(x=>x.subject.name||x.subject.id).join(', '));const st=read(SETTINGS,{visibleSubjects:[],sectionPrefs:{}});st.visibleSubjects??=[];r.subjects.forEach(({subject:s})=>{if(!st.visibleSubjects.includes(s.id))st.visibleSubjects.push(s.id)});write(SETTINGS,st);sessionStorage.setItem('tes_v755_import_result',JSON.stringify({ok:r.subjects.map(x=>x.subject.name||x.subject.id),errors:r.errors}));location.reload()}catch(e){const box=$('#v755BulkStatus');box.hidden=false;box.className='v755bulkstatus bad';box.textContent='적용 실패\n'+(e.message||e);btn.disabled=false;btn.textContent='일괄 적용'}}
function showResult(){const raw=sessionStorage.getItem('tes_v755_import_result');if(!raw)return;sessionStorage.removeItem('tes_v755_import_result');try{const r=JSON.parse(raw),msg=[`본문 JSON ${r.ok.length}과목 적용 완료`,...r.ok.map(x=>'✓ '+x)];if(r.errors?.length)msg.push('',...r.errors.map(x=>'✕ '+x));setTimeout(()=>alert(msg.join('\n')),180)}catch{}}
function enhance(){css();inject();showResult()}new MutationObserver(enhance).observe(document.documentElement,{childList:true,subtree:true});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',enhance);else enhance();
})();